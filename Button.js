export default function Button() {
  return (
    <button
      style={{
        color: "red",
        fontSize: "20px",
        backgroundColor: "blue",
        padding: "10px",
        border: "none",
        borderRadius: "5px",
        cursor: "pointer",
      }}
    >
      Click me
    </button>
  );
}
